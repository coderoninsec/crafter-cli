"""Crafter CLI package.

The package itself intentionally exposes very little. The public entry point is
``crafter.cli:main`` so the tool behaves like a standard console application
when installed with ``pipx``.
"""
